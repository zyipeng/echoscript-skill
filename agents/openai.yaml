interface:
  display_name: "EchoScript"
  short_description: "Local transcription, translation, summaries, and chosen exports"
  default_prompt: "Use $echoscript to process this media locally; infer requested content and export scope, then ask once for any missing choices."
